# Contributing

Use a feature branch, run `ruff check .` and `pytest`, and do not commit credentials or generated document uploads. Keep tenant authorization checks in the service/API boundary for every new workspace resource.
